# =============================================================================
# Python Dependencies - GraspGen Thesis Pipeline
# These are ADDITIONAL to what SAM3 and GraspGen install via their own setup.
# PyTorch 2.7 + CUDA 12.6 is installed separately in the Dockerfile.
# SAM3 and GraspGen are installed from source (pip install -e .)
# =============================================================================

# --- Pinned shared deps (compatible between SAM3 & GraspGen) ---
numpy==1.26.4
transformers>=4.35.0
huggingface_hub>=0.25.2
timm>=1.0.17
scipy>=1.10.0
Pillow>=9.5.0

# --- GraspGen additional deps ---
hydra-core
h5py
pickle5
webdataset
scikit-learn
tensorboard
tensorboardx
trimesh==4.5.3
diffusers==0.11.1
PyOpenGL==3.1.5
addict
yapf==0.40.1
sharedarray
yourdfpy==0.0.56
imageio
meshcat
msgpack
msgpack-numpy

# --- SAM3 additional deps ---
ftfy==6.1.1
regex

# --- Computer Vision & 3D Processing ---
opencv-python>=4.8.0
open3d>=0.17.0
pyrender>=0.1.45

# --- Robot Control ---
roboticstoolbox-python>=1.0.0

# --- ROS2 Python tools ---
pyyaml>=6.0

# --- Utilities ---
tqdm>=4.65.0
matplotlib>=3.7.0

# --- Development & Testing ---
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
